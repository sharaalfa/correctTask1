package a.sharafutdinov;
import java.io.FileNotFoundException;
import java.io.IOException;
import java.io.ObjectInputStream;
import java.util.HashMap;
import java.util.Scanner;
import java.util.TreeMap;

import static java.lang.Thread.sleep;
/**
 * Created by innopolis on 11.02.17.
 */
public class Manager {
    /*TreeMap<String, Integer> amap = new TreeMap<>();
    Scanner scanner;
    public void pars() throws FileNotFoundException {
        ResourceReader resourceReader = new ResourceReader();
        String text = resourceReader.read("example.txt");
        try

        {
            scanner = new Scanner("/home/innopolis/Yandex.Disk/Java_study/runTaskOne/example.txt");

            scanner.useDelimiter("^[-А-яЁё]+");

            while (scanner.hasNext()) {
                String word = scanner.next().toLowerCase();

                putD(word);
                scanner.close();
            }
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
    }

    private void putD(String wrd){
        Integer countWord;
        //countWord=0;
        countWord = amap.get(wrd);
        System.out.println(countWord);
        if(amap.containsKey(wrd)){
            amap.put(wrd, 1);} else{
            amap.put(wrd, countWord=countWord+1);
        }*/



}
